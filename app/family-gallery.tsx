/**
 * Family Gallery — full-screen swipeable gallery of parent's children.
 * Navigated to from the compact family entry point on parent home.
 */
import React from 'react';
import FamilyGallery from '@/components/FamilyGallery';

export default function FamilyGalleryScreen() {
  return <FamilyGallery />;
}
